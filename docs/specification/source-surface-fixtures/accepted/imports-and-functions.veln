use io
use time
use foo::bar from "github.com/oakcask/foo"

pub fn main() -> Result<(), String> effects [stdio]
	require ready == true
	ensure result == Ok(())
	let message: String = "hello"
	io::println(message)
	Ok(())
end

fn helper(value: Int) -> Int
	value + 1
end
