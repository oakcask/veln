mod app.main
use platform.io
use platform.time

pub fn main() -> Result<(), String> effects [stdio]
	require ready == true
	ensure result == Ok(())
	let message: String = "hello"
	io::println(message)
	Ok(())
end

fn helper(value: Int) -> Int
	value + 1
end
