use app::wire

schema Header
  format binary

  length: UInt8
end

codec HeaderDecode for Header decode
  derive decode
end

pub codec HeaderRoundTrip for Header decode encode
  decode with decode_header
  derive encode
end

codec ImportedHeaderDecode for app::wire::Header decode
  derive decode
end
