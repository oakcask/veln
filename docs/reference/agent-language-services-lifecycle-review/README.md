---
role: reference
authority: supporting
update-when: The reviewed agent-language-services source-decision authority or the frozen lifecycle artifact set changes.
---

# Agent Language Services Lifecycle Review

This directory holds the reviewed source-decision authority consumed by the
frozen lifecycle artifact set.

`source-decisions.json` is not a generated migration destination. It binds the
umbrella proposal source digest, root classifications, leaf lifecycle decisions,
and source-bound finite identities that the frozen inventory must match.
