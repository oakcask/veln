# HTTP/2 Standard Library Completion and Fixture Retirement

Status: superseded

This former umbrella completion record is not a completed-proposal authority.
It is retained only so older implemented-slice records that mention
`http2-sans-io-protocol-core.md` keep a stable route.

The broad `http2-protocol-core` fixture was retired before the ordered receive
boundary and evidence-classification gates were fully closed. Current HTTP/2
behavior starts in `../../specification/http2.md`. The remaining planned work
is routed from
`../../proposals/http2-sans-io-protocol-core.md`.

Do not cite this page as proof that preface integration, initial peer
SETTINGS integration, inbound PRIORITY, inbound PING output integration, or
the deleted aggregate evidence matrix are complete.
