name: test / rust

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/test--rust.yaml
      - .config/nextest.toml
      - .github/actions/cargo-dependencies/action.yaml
      - .github/actions/nextest/action.yaml
      - Cargo.lock
      - Cargo.toml
      - rust-toolchain.toml
      - crates/**/Cargo.toml
      - crates/**/*.rs
      - tools/**/Cargo.toml
      - tools/**/*.rs
      - workflow-scripts/plan-nextest-shards.mjs
      - workflow-scripts/prune-rust-test-build-caches.mjs
  pull_request:
    paths:
      - .github/workflows/test--rust.yaml
      - .config/nextest.toml
      - .github/actions/cargo-dependencies/action.yaml
      - .github/actions/nextest/action.yaml
      - Cargo.lock
      - Cargo.toml
      - rust-toolchain.toml
      - crates/**/Cargo.toml
      - crates/**/*.rs
      - tools/**/Cargo.toml
      - tools/**/*.rs
      - workflow-scripts/plan-nextest-shards.mjs
      - workflow-scripts/prune-rust-test-build-caches.mjs

permissions:
  actions: read
  contents: read

env:
  CARGO_TERM_COLOR: always
  # renovate: datasource=github-releases depName=nextest-rs/nextest versioning=semver extractVersion=^cargo-nextest-(?<version>.*)$
  NEXTEST_VERSION: "0.9.140"
  # renovate: datasource=github-releases depName=taiki-e/cargo-llvm-cov
  LLVM_COV_VERSION: "0.9.0"
  LCOV_VERSION: "2.0-4ubuntu2"
  # Keep test execution near twice the clean-build cost so extra shards still repay their build overhead.
  NEXTEST_TARGET_SECONDS: "90"
  NEXTEST_FALLBACK_SHARDS: "4"
  NEXTEST_MAX_SHARDS: "16"

jobs:
  plan-test-shards:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      shard_count: ${{ steps.plan.outputs.shard_count }}
      shards: ${{ steps.plan.outputs.shards }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - name: Plan Rust test shards
        id: plan
        env:
          GH_TOKEN: ${{ github.token }}
        run: node workflow-scripts/plan-nextest-shards.mjs

  seed-cache:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      rust-test-build-cache-key: ${{ steps.seed.outputs.rust-test-build-cache-key }}
    concurrency:
      group: rust-test-main-cache
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - name: Seed Rust test caches
        id: seed
        uses: ./.github/actions/nextest
        with:
          nextest-version: ${{ env.NEXTEST_VERSION }}
          llvm-cov-version: ${{ env.LLVM_COV_VERSION }}
          save-cache: true

  prune-cache:
    needs: seed-cache
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      actions: write
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - name: Delete obsolete Rust test build caches
        env:
          GH_TOKEN: ${{ github.token }}
          RUST_TEST_BUILD_CACHE_KEY: ${{ needs.seed-cache.outputs.rust-test-build-cache-key }}
          RUST_TEST_BUILD_CACHE_PREFIX: rust-test-build-${{ runner.os }}-
        run: node workflow-scripts/prune-rust-test-build-caches.mjs

  test:
    name: test shard ${{ matrix.shard }}
    needs: plan-test-shards
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJSON(needs.plan-test-shards.outputs.shards) }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - name: Run Rust tests
        uses: ./.github/actions/nextest
        with:
          nextest-version: ${{ env.NEXTEST_VERSION }}
          llvm-cov-version: ${{ env.LLVM_COV_VERSION }}
          shard: ${{ matrix.shard }}
          shard-count: ${{ needs.plan-test-shards.outputs.shard_count }}

  coverage:
    name: merge Rust test coverage
    needs: test
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - name: Download Rust shard coverage reports
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: nextest-coverage-*
          path: target/coverage-shards
      - name: Install LCOV and merge every Rust coverage shard
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends "lcov=${LCOV_VERSION}"
          mkdir -p target/coverage
          lcov --branch-coverage \
            --add-tracefile 'target/coverage-shards/*/lcov.info' \
            --output-file target/coverage/lcov.info
      - name: Generate browsable Rust coverage report
        run: |
          genhtml target/coverage/lcov.info \
            --output-directory target/coverage/html \
            --prefix "${GITHUB_WORKSPACE}" \
            --title "Veln Rust test coverage" \
            --legend
      - name: Upload merged Rust coverage reports
        id: upload-coverage
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: rust-coverage
          path: |
            target/coverage/lcov.info
            target/coverage/html
          if-no-files-found: error
      - name: Link browsable Rust coverage report
        env:
          COVERAGE_ARTIFACT_URL: ${{ steps.upload-coverage.outputs.artifact-url }}
        run: echo "Download [rust-coverage](${COVERAGE_ARTIFACT_URL}), extract it, and open html/index.html to inspect covered and missed lines." >> "${GITHUB_STEP_SUMMARY}"

  doc-test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - name: Prepare Cargo dependencies
        uses: ./.github/actions/cargo-dependencies
      - name: Run Rust doctests
        run: cargo test --locked --workspace --doc
