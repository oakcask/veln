command = ["fmt", "--help"]
exit = 0

[stdout]
format = "text"

[stderr]
format = "empty"

[help]
summary = "Format source files"
usage = "veln fmt [INPUTS]..."
arguments = ["  [INPUTS]...  Source files or directories to format"]
options = ["  -h, --help  Print help"]
