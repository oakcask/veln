command = ["test"]
exit = 1

[tools]
java = "missing"

[stdout]
format = "text"
contains = ["error passes\n"]

[stderr]
format = "text"
contains = ["veln: test `passes` failed: veln: `java` was not found; install a JDK to use `veln test`"]
