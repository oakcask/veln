# Cancellable wait deadline expiry remains transport runtime blame.
pub fn main() -> Result<(), String> effects [time, stdio]
	let deadline: Deadline = time::deadline_after_ms(5)
	let token: CancelToken = time::cancel_token()
	time::wait_until_cancellable(deadline, token)
	stdio::println("unreachable")
	Ok(())
end
