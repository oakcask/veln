# Binary schema ByteView product encode rejects computed length mismatches.
use stdio

schema PacketWire
	format binary

	row_count: UInt8
	column_count: UInt8
	payload: ByteView(row_count * column_count)
end

fn emit_error(error: EncodeError) -> () effects [stdio]
	stdio::println(error_id(error))
	stdio::println(error_path(error))
	stdio::println(error_reason(error))
end

fn error_id(error: EncodeError) -> String
	match error
		EncodeError(id, _, _) => id
	end
end

fn error_path(error: EncodeError) -> String
	match error
		EncodeError(_, field_path, _) => field_path
	end
end

fn error_reason(error: EncodeError) -> String
	match error
		EncodeError(_, _, reason) => reason
	end
end

pub fn main() -> Result<(), String> effects [stdio]
	let payload_bytes = byte_chunk_from_hex("aabbccdd")?
	let payload = byte_view(payload_bytes, byte_offset(0)?, byte_count(4)?)?
	let encoded = byte_encode_packet_wire({row_count: 2, column_count: 3, payload: payload})
	let emitted = match encoded
		Ok(_) => stdio::println("unexpected encode success")
		Err(error) => emit_error(error)
	end
	Ok(())
end
