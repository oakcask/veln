command = ["run", "main", "main.veln", "hpack_fixture.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["error[hpack.fixture.unsupported_static_index]: unsupported HPACK static index at byte offset 27", "note: HPACK fixture codec `hpack_fixture` observed header block size 1 and first byte 128.", "note: Nearby bytes: 80 (showing 1 of 1 byte(s), complete).", "note: Expected fixture HPACK static indexed header."]
