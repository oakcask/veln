# Listener address metadata failures stay runtime transport failures in run JSON.
pub fn main() -> Result<(), String> effects [net]
	let listener: NetListener = net::listen("127.0.0.1:0")
	let local: String = net::listener_local_addr(listener)
	Ok(())
end
