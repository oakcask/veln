command = ["run", "--json", "main", "main.veln", "wire.veln", "facade.veln"]
exit = 0

[manifest_error]
contains = ["unresolved binary_fixture 0 schema reference `MissingPacket`", "binary_fixture 1 schema reference `wire::PrivatePacket` is private", "binary_fixture 2 schema reference `wire::make_packet` is a function, not a schema", "binary_fixture 3 schema reference `wire::PacketShape` is a type, not a schema", "unresolved binary_fixture 4 schema reference `wire::PacketCodec`", "unresolved binary_fixture 5 schema reference `wire::byte_decode_public_packet`", "unresolved binary_fixture 6 schema reference `other::PublicPacket`", "binary_fixture 7 `field_path` first segment must name schema `PublicPacket`"]

[[binary_fixture]]
name = "missing-schema"
schema = "MissingPacket"
hex = "00"
diagnostic_id = "schema.invalid_field_value"
byte_offset = 0
field_path = [{"kind":"schema","name":"MissingPacket"}]

[[binary_fixture]]
name = "private-schema"
schema = "wire::PrivatePacket"
hex = "00"
diagnostic_id = "schema.invalid_field_value"
byte_offset = 0
field_path = [{"kind":"schema","name":"PrivatePacket"}]

[[binary_fixture]]
name = "function-target"
schema = "wire::make_packet"
hex = "00"
diagnostic_id = "schema.invalid_field_value"
byte_offset = 0
field_path = [{"kind":"schema","name":"make_packet"}]

[[binary_fixture]]
name = "type-target"
schema = "wire::PacketShape"
hex = "00"
diagnostic_id = "schema.invalid_field_value"
byte_offset = 0
field_path = [{"kind":"schema","name":"PacketShape"}]

[[binary_fixture]]
name = "codec-target"
schema = "wire::PacketCodec"
hex = "00"
diagnostic_id = "schema.invalid_field_value"
byte_offset = 0
field_path = [{"kind":"schema","name":"PacketCodec"}]

[[binary_fixture]]
name = "generated-helper-target"
schema = "wire::byte_decode_public_packet"
hex = "00"
diagnostic_id = "schema.invalid_field_value"
byte_offset = 0
field_path = [{"kind":"schema","name":"PublicPacket"}]

[[binary_fixture]]
name = "missing-use"
schema = "other::PublicPacket"
hex = "00"
diagnostic_id = "schema.invalid_field_value"
byte_offset = 0
field_path = [{"kind":"schema","name":"PublicPacket"}]

[[binary_fixture]]
name = "field-path-mismatch"
schema = "wire::PublicPacket"
hex = "00"
diagnostic_id = "schema.invalid_field_value"
byte_offset = 0
field_path = [{"kind":"schema","name":"OtherPacket"}]
