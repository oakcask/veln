command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["error[http2.peer_limit.header_list_size_exceeded]: header list size exceeds receive maximum at byte offset 12", "note: Frame kind 9 on stream 1 decoded header list size 10; active receive maximum is 9.", "note: Receive limit provenance: local_configuration.", "note: Rule provenance: header_list_receive_limit."]
