command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["error[schema.integer_out_of_range]: schema integer out of range at byte offset 0", "note: 4-byte schema integer expected value between 0 and 2147483647; actual value was 2147483648.", "note: Nearby bytes: 80 00 00 00 (showing 4 of 4 byte(s), complete).", "note: Field path: schema `StreamIdentifierSample` / field `stream_id`."]
