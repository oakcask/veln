command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["error[schema.reserved_bits_mismatch]: reserved bits mismatch at byte offset 5", "note: ReservedBits(1, 0) expected value 0; actual value was 1.", "note: Nearby bytes: 00 00 05 01 04 80 00 00 (showing 8 of 9 byte(s), truncated).", "note: Field path: schema `Http2FrameHeader` / field `stream_reserved`."]
