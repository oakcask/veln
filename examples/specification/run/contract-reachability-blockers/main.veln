# Selected run reachability follows pure helpers used only by contract predicates.
fn accepts(job: fn() -> Bool effects []) -> Bool effects []
  job()
end

fn ready() -> Bool effects []
  _
end

pub fn main() -> () effects []
require accepts(ready)
  ()
end
