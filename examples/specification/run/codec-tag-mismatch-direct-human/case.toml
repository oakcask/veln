command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["error[codec.tag_mismatch]: tag mismatch at byte offset 14", "note: Field path: schema `ManualPacketWire` / field `kind`.", "note: Expected tag `DATA`; actual tag was `HEADERS`.", "note: Tag mismatch reason: dispatch tag did not match selected payload.", "note: DecodeError value: DecodeErrorWithReason(codec.tag_mismatch, ByteOffset(14), ManualPacketWire.kind, expected_tag=DATA; actual_tag=HEADERS; reason=dispatch tag did not match selected payload)."]
