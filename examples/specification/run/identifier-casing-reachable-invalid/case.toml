command = ["run", "main", "main.veln"]
source_errors = "expected"
exit = 1

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["error[name.invalid_case]: function name must start with an ASCII lowercase letter", "main.veln:1:4"]
