# Binary schema frame-header decode reports human context for a truncated field.
pub fn main() -> Result<(), String>
	let bytes: ByteChunk = byte_chunk_from_hex("000005010400")?
	let offset: ByteOffset = byte_offset(0)?
	let count: ByteCount = byte_count(6)?
	let view: ByteView = byte_view(bytes, offset, count)?
	let ignored: {length: Int, kind: Int, flags: Int, stream_id: Int} = byte_decode_http2_frame_header(view)?
	Ok(())
end
