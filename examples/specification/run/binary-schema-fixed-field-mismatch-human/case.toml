command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
contains = ["error[schema.fixed_field_mismatch]: fixed field mismatch at byte offset 0", "note: Fixed field expected value 1; actual value was 255.", "note: Nearby bytes: ff 00 01 (showing 3 of 3 byte(s), complete).", "note: Field path: schema `DemoPacket` / field `kind`."]
