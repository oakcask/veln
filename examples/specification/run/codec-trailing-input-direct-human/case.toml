command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["error[codec.trailing_input]: trailing input at byte offset 5", "note: Field path: schema `ManualPacketWire` / field `payload`.", "note: Consumed 5 of 8 available bytes; 3 bytes remain.", "note: Trailing input reason: packet decoder completed before the bounded input ended.", "note: DecodeError value: DecodeErrorWithReason(codec.trailing_input, ByteOffset(5), ManualPacketWire.payload, consumed_count=5; available_count=8; remaining_count=3; reason=packet decoder completed before the bounded input ended)."]
