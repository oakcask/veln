// Match arms are evaluated in source order, including catch-all arms.
pub fn main() -> () effects [stdio]
  let label: String = match Some(1)
    _ => "first"
    Some(_) => "second"
  end
  stdio::println(label)
end
