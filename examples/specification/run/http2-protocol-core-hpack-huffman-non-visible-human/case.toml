command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["error[hpack.fixture.huffman_non_visible_value]: HPACK Huffman decoded non-visible header value at byte offset 9", "note: HPACK fixture codec `hpack_static` observed header block size 6 and first byte 4.", "note: Nearby bytes: 04 84 ff c7 fe 3f (showing 6 of 6 byte(s), complete).", "note: Expected fixture HPACK Huffman checked header value."]
