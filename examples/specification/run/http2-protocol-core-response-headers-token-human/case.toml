command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
contains = ["error[http2.protocol.invalid_response_header_list]: response header list contains invalid ordinary header name bad header at byte offset 12", "note: Frame kind 1 on stream 1 decoded response header names: :status,bad header.", "note: Active protocol state: response-headers.", "note: Rule provenance: rfc9110_field_name_token."]
