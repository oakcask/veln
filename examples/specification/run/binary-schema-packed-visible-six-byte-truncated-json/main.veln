schema PackedVisibleSixByteHeader
	format binary

	high: UInt2
	upper: UInt7
	middle: UInt4
	lower: UInt7
	tail: UInt7
	flag: UInt7
	code: UInt7
	route: UInt7
end

pub fn main() -> Result<(), String>
	let bytes: ByteChunk = byte_chunk_from_hex("abcdef12")?
	let view: ByteView = byte_view(bytes, byte_offset(0)?, byte_count(4)?)?
	let ignored: {high: Int, upper: Int, middle: Int, lower: Int, tail: Int, flag: Int, code: Int, route: Int} = byte_decode_packed_visible_six_byte_header(view)?
	Ok(())
end
