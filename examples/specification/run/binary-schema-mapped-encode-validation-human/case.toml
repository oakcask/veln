command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
contains = ["error[schema.validation_failed]: schema encode validation failed", "note: Predicate `wire_padding_length <= wire_length` failed for supplied field value 6.", "note: Supplied values: wire_length=5, wire_padding_length=6.", "note: Field path: schema `HeaderWire` / field `wire_padding_length`.", "note: Result value: EncodeError(schema.validation_failed, HeaderWire.wire_padding_length, schema encode validation failed for field `wire_padding_length`)."]
