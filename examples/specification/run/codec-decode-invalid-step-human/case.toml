command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
contains = ["error[codec.invalid_input]: decode error at byte offset 5", "note: Field path: schema `ManualPacketWire` / field `kind`.", "note: DecodeError value: DecodeError(codec.invalid_input, ByteOffset(5), ManualPacketWire.kind)."]
