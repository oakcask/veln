# Source-visible DecodeStep Invalid entries project focused human diagnostics.

pub fn main() -> DecodeStep<Int>
	match byte_offset(5)
		Ok(offset) => Invalid(DecodeError("codec.invalid_input", offset, "ManualPacketWire.kind"))
		Err(_) => NeedMore(NeedEnd)
	end
end
