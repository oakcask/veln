command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["error[http2.peer_limit.flow_control_window_exceeded]: flow-control window exceeded at byte offset 0", "note: Frame kind 0 on stream 1 declared 4 byte(s); available receive window credit is 3 byte(s).", "note: Active protocol state: open-stream.", "note: Rule provenance: stream_receive_window."]
