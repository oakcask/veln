command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["Err(fixture.hex.odd_length: dangling hex nibble at byte offset 1 high nibble)"]
