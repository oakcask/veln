# Ensure clauses are checked before question-mark early returns.
fn fail() -> Result(Int, String) effects []
  Err("boom")
end

pub fn main() -> Result(Int, String) effects []
ensure false
  let value: Int = fail()?
  Ok(value)
end
