command = ["run", "main", "main.veln", "--", "64"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["invalid shift count 64 for operator `<<`; expected a value between 0 and 63"]
