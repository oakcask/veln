command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
format = "text"
contains = ["error[http2.protocol.invalid_payload_length]: invalid payload length at byte offset 0", "note: Frame kind 4 on connection 0 declared 6 byte(s); expected 0 byte(s).", "note: Active protocol state: connection-control.", "note: Rule provenance: rfc9113_settings_ack_payload_length."]
