# HTTP/2 PUSH_PROMISE promised request header-list diagnostics keep frame context.
fn invalid_payload(error: String) -> RuntimeDiagnostic
	RuntimeDiagnostic(
		"http2.protocol.invalid_request_header_list",
		error,
		RuntimeHttp2ProtocolInvalidRequestHeaderListDiagnostic(0, 5, 1, "response_only_pseudo_header", ":status", ":status,:method,:scheme,:path", "request-headers", "rfc9113_request_pseudo_headers", byte_chunk([]))
	)
end

fn preview_bytes() -> Result<ByteChunk, RuntimeDiagnostic>
	match byte_chunk_from_hex("32333435")
		Ok(bytes) => Ok(bytes)
		Err(error) => Err(invalid_payload(error))
	end
end

fn preview_view(bytes: ByteChunk) -> Result<ByteView, RuntimeDiagnostic>
	match byte_offset(0)
		Ok(offset) => match byte_count(4)
			Ok(count) => match byte_view(bytes, offset, count)
				Ok(view) => Ok(view)
				Err(error) => Err(invalid_payload(error))
			end
			Err(error) => Err(invalid_payload(error))
		end
		Err(error) => Err(invalid_payload(error))
	end
end

pub fn main() -> Result<(), RuntimeDiagnostic>
	let bytes: ByteChunk = preview_bytes()?
	http2_protocol_invalid_request_header_list(0, 5, 1, "response_only_pseudo_header", ":status", ":status,:method,:scheme,:path", "request-headers", "rfc9113_request_pseudo_headers", preview_view(bytes)?)
end
