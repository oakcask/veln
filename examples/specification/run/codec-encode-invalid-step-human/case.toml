command = ["run", "main", "main.veln"]
exit = 1

[requires]
jdk = true

[stdout]
format = "empty"

[stderr]
contains = ["error[codec.encode_value_unrepresentable]: encode value is unrepresentable", "note: Field path: schema `ManualPacketWire` / field `kind`.", "note: Encode failure reason: kind must be less than or equal to length.", "note: Result value: EncodeError(codec.encode_value_unrepresentable, ManualPacketWire.kind, kind must be less than or equal to length)."]
