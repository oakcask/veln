command = ["package", "lock"]
exit = 1

[tools]
git = "fake-git-rev-parse"

[stdout]
format = "empty"

[stderr]
contains = ["vendor/zeta/veln.toml:3:16: error[package.incompatible_dependency_source]: dependency `github.com/oakcask/shared` selects git `vendor/mono` with rev `abc123` in subdir `packages/two`, but that package identity is already selected as git `vendor/mono` with rev `abc123` in subdir `packages/one`", "note: vendor/alpha/veln.toml:3:16: This dependency selected the package identity first."]
