command = ["package", "lock"]
exit = 1

[stdout]
format = "empty"

[stderr]
contains = ["vendor/zeta/veln.toml:3:16: error[package.incompatible_dependency_source]: dependency `github.com/oakcask/shared` selects vendor `vendor/shared`, but that package identity is already selected as path `vendor/shared`", "note: vendor/alpha/veln.toml:3:16: This dependency selected the package identity first."]
