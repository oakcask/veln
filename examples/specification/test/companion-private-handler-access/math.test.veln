use math

test companion_handles_with_private_target_handler() -> Result<(), String>
	let observed = handle math::compute() with math::ask(41)
	if observed == 42
		Ok(())
	else
		Err("handler returned unexpected value")
	end
end
