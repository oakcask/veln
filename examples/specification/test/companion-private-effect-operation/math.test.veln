use math

fn provide(offset: Int) -> Int
	offset + 1
end

handler ask(offset: Int) handles math::Ask
	value() => provide(offset)
end

test companion_performs_private_target_effect() -> ()
	let observed: Int = handle perform math::Ask::value() with ask(40)
	()
end
