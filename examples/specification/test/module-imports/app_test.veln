# Test analysis uses imported qualified calls from selected companion files.
mod spec.app_test
use spec.math

test qualified_import_call() -> () effects [stdio]
  stdio::println(int_to_string(math::double(5)))
end
