command = ["metrics", "--write-baseline", "metrics.baseline.json"]
exit = 0

[stdout]
contains = ["wrote metrics baseline: metrics.baseline.json"]

[stderr]
format = "empty"

[[file_assert]]
path = "metrics.baseline.json"
equals_file = "case-text/file-assert-equals-1.txt"
