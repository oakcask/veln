command = ["check", "--json", "main.veln"]
exit = 1

[stdout]
format = "json"
contains = ["\"id\":\"type.mismatch\"", "expected `fn(Int) -> String`, but found `fn(String) -> String`", "\"id\":\"type.private_inference_incomplete\"", "private parameter `value` has no inferred type", "private parameter `fixed` has no inferred type", "constructor `Some` needs type context", "constructor `Ok` needs type context", "constructor `Err` needs type context"]

[stderr]
format = "empty"

[[json_assert]]
path = "status"
equals = "error"

[[json_assert]]
path = "summary.diagnostic_count"
equals = 11

[[diagnostics]]
id = "type.private_inference_incomplete"
severity = "error"
kind = "type"
message = "private parameter `value` has no inferred type"

[diagnostics.span]
file = "main.veln"
line = 19
column = 20

[[diagnostics]]
id = "type.private_inference_incomplete"
severity = "error"
kind = "type"
message = "private parameter `fixed` has no inferred type"

[diagnostics.span]
file = "main.veln"
line = 19
column = 27

[[diagnostics]]
id = "type.mismatch"
severity = "error"
kind = "type"
message = "expected `fn(Int) -> String`, but found `fn(String) -> String`"

[diagnostics.span]
file = "main.veln"
line = 36
column = 12

[[diagnostics]]
id = "type.inference_ambiguous"
severity = "error"
kind = "type"
message = "constructor `GenericProcessor` needs type context"

[diagnostics.span]
file = "main.veln"
line = 40
column = 18

[[diagnostics]]
id = "type.mismatch"
severity = "error"
kind = "type"
message = "expected `fn(Int) -> String`, but found `fn(String) -> String`"

[diagnostics.span]
file = "main.veln"
line = 45
column = 7

[[diagnostics]]
id = "type.private_inference_incomplete"
severity = "error"
kind = "type"
message = "private parameter `value` has no inferred type"

[diagnostics.span]
file = "main.veln"
line = 23
column = 27

[[diagnostics]]
id = "type.private_inference_incomplete"
severity = "error"
kind = "type"
message = "private parameter `value` has no inferred type"

[diagnostics.span]
file = "main.veln"
line = 27
column = 23

[[diagnostics]]
id = "type.private_inference_incomplete"
severity = "error"
kind = "type"
message = "private parameter `value` has no inferred type"

[diagnostics.span]
file = "main.veln"
line = 31
column = 24

[[diagnostics]]
id = "type.inference_ambiguous"
severity = "error"
kind = "type"
message = "constructor `Some` needs type context"

[diagnostics.span]
file = "main.veln"
line = 49
column = 18

[[diagnostics]]
id = "type.inference_ambiguous"
severity = "error"
kind = "type"
message = "constructor `Ok` needs type context"

[diagnostics.span]
file = "main.veln"
line = 54
column = 16

[[diagnostics]]
id = "type.inference_ambiguous"
severity = "error"
kind = "type"
message = "constructor `Err` needs type context"

[diagnostics.span]
file = "main.veln"
line = 55
column = 16
