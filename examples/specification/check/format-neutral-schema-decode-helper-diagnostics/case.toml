command = ["check", "--json", "main.veln"]
exit = 1

[stdout]
format = "json"
contains = ["\"id\":\"schema.format_neutral_decode_helper\"", "format-neutral schema field `numeric_scores` cannot expose a generated decode helper", "\"field_type\":\"Dict<Int, Int>\"", "format-neutral schema field `optional_scores` cannot expose a generated decode helper", "\"field_type\":\"Option<Dict<Int, Int>>\"", "format-neutral schema field `nested_optional_scores` cannot expose a generated decode helper", "\"field_type\":\"{ scores : Option<Dict<Int, Int>> }\"", "format-neutral schema field `result_items` cannot expose a generated decode helper", "\"field_type\":\"Result<List<Int>, String>\"", "format-neutral schema field `result_labels` cannot expose a generated decode helper", "\"field_type\":\"Result<Int, Dict<String, String>>\"", "format-neutral schema field `vec_items` cannot expose a generated decode helper", "\"field_type\":\"Vec<Int>\"", "format-neutral schema field `local_payload` cannot expose a generated decode helper", "\"field_type\":\"LocalPayload\"", "format-neutral schema field `callback` cannot expose a generated decode helper", "\"field_type\":\"fn(Int) -> String\"", "format-neutral schema field `metadata` cannot expose a generated decode helper", "\"field_type\":\"{ payload : LocalPayload, callback : fn(Int) -> String }\"", "\"reason\":\"unsupported_format_neutral_field_type\"", "recursive format-neutral visible shape", "Result<scalar, scalar>", "Generated format-neutral decode helpers for schema `BadPacket`"]

[stderr]
format = "empty"

[[json_assert]]
path = "status"
equals = "error"

[[json_assert]]
path = "summary.diagnostic_count"
equals = 9

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `numeric_scores` cannot expose a generated decode helper because `Dict<Int, Int>` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, or Result<scalar, scalar>"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `optional_scores` cannot expose a generated decode helper because `Option<Dict<Int, Int>>` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, or Result<scalar, scalar>"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `nested_optional_scores` cannot expose a generated decode helper because `{ scores : Option<Dict<Int, Int>> }` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, or Result<scalar, scalar>"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `result_items` cannot expose a generated decode helper because `Result<List<Int>, String>` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, or Result<scalar, scalar>"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `result_labels` cannot expose a generated decode helper because `Result<Int, Dict<String, String>>` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, or Result<scalar, scalar>"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `vec_items` cannot expose a generated decode helper because `Vec<Int>` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, or Result<scalar, scalar>"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `local_payload` cannot expose a generated decode helper because `LocalPayload` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, or Result<scalar, scalar>"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `callback` cannot expose a generated decode helper because `fn(Int) -> String` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, or Result<scalar, scalar>"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `metadata` cannot expose a generated decode helper because `{ payload : LocalPayload, callback : fn(Int) -> String }` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, or Result<scalar, scalar>"
