schema Packet
  format binary

  kind: UInt8
end

codec WrongReturnDecode for Packet decode
  decode with wrong_return
end

fn wrong_return(input: ByteView, base: ByteOffset) -> Result<Int, String>
  Ok(0)
end
