command = ["check", "--json", "main.veln", "wire.veln"]
exit = 1

[stdout]
format = "json"
contains = ["\"id\":\"schema.format_neutral_decode_helper\"", "format-neutral schema field `callback_payload` cannot expose a generated decode helper", "\"field_type\":\"CallbackPayload\"", "format-neutral schema field `optional_vec_payload` cannot expose a generated decode helper", "\"field_type\":\"Option<VecPayload>\"", "format-neutral schema field `bad_dict_payload` cannot expose a generated decode helper", "\"field_type\":\"Dict<Int, CallbackPayload>\"", "format-neutral schema field `imported_payload` cannot expose a generated decode helper", "\"field_type\":\"wire::WirePayload\"", "format-neutral schema field `metadata` cannot expose a generated decode helper", "\"field_type\":\"{ payload : CallbackPayload }\"", "\"reason\":\"unsupported_format_neutral_field_type\"", "same-module source ADTs whose constructor payloads are recursive visible shapes", "Generated format-neutral decode helpers for schema `BadPacket`"]

[stderr]
format = "empty"

[[json_assert]]
path = "status"
equals = "error"

[[json_assert]]
path = "summary.diagnostic_count"
equals = 5

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `callback_payload` cannot expose a generated decode helper because `CallbackPayload` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module source ADTs whose constructor payloads are recursive visible shapes"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `optional_vec_payload` cannot expose a generated decode helper because `Option<VecPayload>` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module source ADTs whose constructor payloads are recursive visible shapes"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `bad_dict_payload` cannot expose a generated decode helper because `Dict<Int, CallbackPayload>` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module source ADTs whose constructor payloads are recursive visible shapes"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `imported_payload` cannot expose a generated decode helper because `wire::WirePayload` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module source ADTs whose constructor payloads are recursive visible shapes"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `metadata` cannot expose a generated decode helper because `{ payload : CallbackPayload }` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module source ADTs whose constructor payloads are recursive visible shapes"
