command = ["check", "--json", "main.veln", "wire.veln"]
exit = 1

[stdout]
format = "json"
contains = ["\"id\":\"schema.format_neutral_decode_helper\"", "\"id\":\"schema.format_neutral_encode_helper\"", "format-neutral schema field `callback_payload` cannot expose a generated decode helper", "format-neutral schema field `callback_payload` cannot expose a generated encode helper", "\"field_type\":\"CallbackPayload\"", "format-neutral schema field `optional_vec_payload` cannot expose a generated decode helper", "\"field_type\":\"Option<VecPayload>\"", "format-neutral schema field `bad_dict_payload` cannot expose a generated decode helper", "\"field_type\":\"Dict<Int, CallbackPayload>\"", "format-neutral schema field `private_imported_payload` cannot expose a generated decode helper", "format-neutral schema field `private_imported_payload` cannot expose a generated encode helper", "\"field_type\":\"wire::PrivateWirePayload\"", "format-neutral schema field `imported_callback_payload` cannot expose a generated decode helper", "format-neutral schema field `imported_callback_payload` cannot expose a generated encode helper", "\"field_type\":\"wire::WireCallbackPayload\"", "format-neutral schema field `missing_imported_payload` cannot expose a generated decode helper", "format-neutral schema field `missing_imported_payload` cannot expose a generated encode helper", "\"field_type\":\"missing::WirePayload\"", "format-neutral schema field `imported_schema_target` cannot expose a generated decode helper", "format-neutral schema field `imported_schema_target` cannot expose a generated encode helper", "\"field_type\":\"wire::WireSchema\"", "format-neutral schema field `metadata` cannot expose a generated decode helper", "\"field_type\":\"{ payload : CallbackPayload }\"", "\"reason\":\"unsupported_format_neutral_field_type\"", "\"reason\":\"unsupported_format_neutral_encode_field_type\"", "Vec<T>", "same-module or public imported source ADTs whose constructor payloads are recursive visible shapes", "Option<List<scalar>>", "same-module or public imported source ADTs referenced through written use paths", "Generated format-neutral decode helpers for schema `BadPacket`", "Generated format-neutral encode helpers for schema `BadPacket`"]

[stderr]
format = "empty"

[[json_assert]]
path = "status"
equals = "error"

[[json_assert]]
path = "summary.diagnostic_count"
equals = 13

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `callback_payload` cannot expose a generated decode helper because `CallbackPayload` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Vec<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module or public imported source ADTs whose constructor payloads are recursive visible shapes"

[[diagnostics]]
id = "schema.format_neutral_encode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `callback_payload` cannot expose a generated encode helper because `CallbackPayload` is not a supported format-neutral encode shape"

[[json_assert]]
path = "diagnostics.1.details.schema"
equals = "BadPacket"

[[json_assert]]
path = "diagnostics.1.details.field"
equals = "callback_payload"

[[json_assert]]
path = "diagnostics.1.details.field_type"
equals = "CallbackPayload"

[[json_assert]]
path = "diagnostics.1.details.reason"
equals = "unsupported_format_neutral_encode_field_type"

[[json_assert]]
path = "diagnostics.1.related.0.kind"
equals = "schema_helper_boundary"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `optional_vec_payload` cannot expose a generated decode helper because `Option<VecPayload>` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Vec<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module or public imported source ADTs whose constructor payloads are recursive visible shapes"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `bad_dict_payload` cannot expose a generated decode helper because `Dict<Int, CallbackPayload>` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Vec<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module or public imported source ADTs whose constructor payloads are recursive visible shapes"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `private_imported_payload` cannot expose a generated decode helper because `wire::PrivateWirePayload` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Vec<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module or public imported source ADTs whose constructor payloads are recursive visible shapes"

[[diagnostics]]
id = "schema.format_neutral_encode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `private_imported_payload` cannot expose a generated encode helper because `wire::PrivateWirePayload` is not a supported format-neutral encode shape"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `imported_callback_payload` cannot expose a generated decode helper because `wire::WireCallbackPayload` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Vec<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module or public imported source ADTs whose constructor payloads are recursive visible shapes"

[[diagnostics]]
id = "schema.format_neutral_encode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `imported_callback_payload` cannot expose a generated encode helper because `wire::WireCallbackPayload` is not a supported format-neutral encode shape"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `missing_imported_payload` cannot expose a generated decode helper because `missing::WirePayload` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Vec<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module or public imported source ADTs whose constructor payloads are recursive visible shapes"

[[diagnostics]]
id = "schema.format_neutral_encode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `missing_imported_payload` cannot expose a generated encode helper because `missing::WirePayload` is not a supported format-neutral encode shape"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `imported_schema_target` cannot expose a generated decode helper because `wire::WireSchema` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Vec<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module or public imported source ADTs whose constructor payloads are recursive visible shapes"

[[diagnostics]]
id = "schema.format_neutral_encode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `imported_schema_target` cannot expose a generated encode helper because `wire::WireSchema` is not a supported format-neutral encode shape"

[[diagnostics]]
id = "schema.format_neutral_decode_helper"
severity = "error"
kind = "type"
message = "format-neutral schema field `metadata` cannot expose a generated decode helper because `{ payload : CallbackPayload }` is not a recursive format-neutral visible shape made from scalar leaves, anonymous record fields, Option<T>, List<T>, Vec<T>, Dict<String, T>, Result<recursive visible shape, recursive visible shape>, or same-module or public imported source ADTs whose constructor payloads are recursive visible shapes"
