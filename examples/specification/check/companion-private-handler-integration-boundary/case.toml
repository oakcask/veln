command = ["check", "--json", "math_test.veln", "math.veln"]
exit = 1

[stdout]
format = "json"
contains = ["\"id\":\"handler.unknown\"", "handler `math::ask` is not known"]

[stderr]
format = "empty"

[[json_assert]]
path = "status"
equals = "error"
