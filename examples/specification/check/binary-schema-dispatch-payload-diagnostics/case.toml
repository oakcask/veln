command = ["check", "--json", "main.veln", "wire.veln"]
exit = 1

[stdout]
format = "json"
contains = ["dispatch payload schema `MissingPayload` is not declared", "dispatch payload `Shape` resolves to a type, not a schema", "imported dispatch payload schema `wire::PrivatePayload` is private", "dispatch payload schema `wire::MissingPayload` is not declared", "dispatch payload `wire::WireShape` resolves to a type, not a schema", "dispatch payload schema `UnsupportedReservedPayload` is outside the generated binary schema helper slice", "dispatch payload schema `UnsupportedReservedSuffixPayload` is outside the generated binary schema helper slice", "dispatch payload schema `UnsupportedByteViewPayload` is outside the generated binary schema helper slice", "dispatch payload schema `UnsupportedMappedPayload` is outside the generated binary schema helper slice", "\"reason\":\"incompatible_payload_schema\"", "\"expected_decode_helper\":\"byte_decode_step_unsupported_reserved_payload\"", "\"expected_encode_helper\":\"byte_encode_unsupported_reserved_payload\"", "\"expected_decode_helper\":\"byte_decode_step_unsupported_byte_view_payload\"", "\"expected_encode_helper\":\"byte_encode_unsupported_byte_view_payload\"", "\"expected_decode_helper\":\"byte_decode_step_unsupported_mapped_payload\"", "\"expected_encode_helper\":\"byte_encode_unsupported_mapped_payload\"", "\"unsupported_nested_field_path\":[{\"kind\":\"schema\",\"name\":\"UnsupportedByteViewPayload\"},{\"kind\":\"field\",\"name\":\"payload\"}]", "\"unsupported_nested_mapping_target_path\":[{\"kind\":\"schema\",\"name\":\"UnsupportedMappedPayload\"},{\"kind\":\"mapping_target\",\"name\":\"code\"}]", "\"unsupported_nested_layout_reason\":\"ineligible_mapping_projection\"", "\"unsupported_nested_layout_fact\":\"`ByteView(later_length)` requires length reference `later_length` to be declared before field `payload`\"", "\"unsupported_nested_layout_fact\":\"mapping assignment `code * 2` cannot be projected back to schema-local fields for generated encode\"", "\"unavailable_helper_directions\":[\"decode\",\"encode\"]", "\"unavailable_helper_directions\":[\"encode\"]", "Nested dispatch payload field `UnsupportedByteViewPayload.payload` prevents generated decode and encode helpers", "Nested dispatch payload mapping `UnsupportedMappedPayload.code` prevents generated encode helpers", "dispatch payload schema `SelfPacket` cannot reference itself", "dispatch payload schema `LaterPayload` must be declared before schema `ForwardPacket`", "dispatch payload schema `ExtensionSelfPacket` cannot reference itself", "dispatch payload case `2` decodes as `{code: Int, value: Int}`, but earlier cases decode as `Int`"]

[stderr]
format = "empty"

[[json_assert]]
path = "status"
equals = "error"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload schema `MissingPayload` is not declared"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload `Shape` resolves to a type, not a schema"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "imported dispatch payload schema `wire::PrivatePayload` is private"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload schema `wire::MissingPayload` is not declared"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload `wire::WireShape` resolves to a type, not a schema"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload schema `UnsupportedReservedPayload` is outside the generated binary schema helper slice"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload schema `UnsupportedReservedSuffixPayload` is outside the generated binary schema helper slice"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload schema `UnsupportedByteViewPayload` is outside the generated binary schema helper slice"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload schema `UnsupportedMappedPayload` is outside the generated binary schema helper slice"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload schema `SelfPacket` cannot reference itself"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload schema `LaterPayload` must be declared before schema `ForwardPacket`"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload schema `ExtensionSelfPacket` cannot reference itself"

[[diagnostics]]
id = "schema.dispatch_payload"
severity = "error"
kind = "type"
message = "dispatch payload case `2` decodes as `{code: Int, value: Int}`, but earlier cases decode as `Int`"
