// Result-returning callbacks on vec_map report the ordinary type error and
// attach the fallible traversal repair hint.
fn stringify(value: Int) -> Result(String, String)
  Ok(int_to_string(value))
end

pub fn main() -> Vec(String)
  vec_map([1], stringify)
end
