type FrameHeader
	FrameHeader {kind: Int, length: Int}
end

schema FrameHeaderWire
	format binary

	wire_length: UInt16be
	wire_kind: UInt8

	map to FrameHeader
		length = missing_length
		kind = wire_kind
end
