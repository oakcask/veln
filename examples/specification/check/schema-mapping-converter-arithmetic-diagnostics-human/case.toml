command = ["check", "main.veln"]
exit = 1

[stdout]
format = "text"
contains = ["error[schema.mapping_converter]: schema mapping converter `missing_converter` is not resolved", "error[schema.mapping_converter_arity]: schema mapping converter `two_params` expects 2 argument(s), but got 1", "error[schema.mapping_converter_input]: schema mapping converter `needs_text` expects `String`, but source field `kind` decodes as `Int`", "error[schema.mapping_converter_return]: schema mapping converter `to_text` returns `String`, but target field `bad_return` expects `Int`", "error[schema.mapping_converter_purity]: schema mapping converter `noisy` must be pure", "error[schema.mapping_expression_unsupported]: schema mapping expression `kind == 1` is not supported"]

[stderr]
format = "empty"
