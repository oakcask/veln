use math

fn wrong_target_decode(view: ByteView, base: ByteOffset) -> DecodeStep<{length: Int}>
	decode math::Packet from view at base
end

schema WrongComposition
	format binary

	nested: math::Packet
end
