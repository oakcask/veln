# Private imported schemas are unavailable through qualified schema decode paths.
use wire

pub fn main(view: ByteView, base: ByteOffset) -> DecodeStep<{length: Int}>
	decode wire::PacketWire from view at base
end
