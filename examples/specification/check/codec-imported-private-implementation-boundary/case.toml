command = ["check", "--json", "main.veln", "wire.veln"]
exit = 1

[stdout]
format = "json"
contains = ["schema decode expression schema `wire::PacketWire` is private"]

[stderr]
format = "empty"

[[json_assert]]
path = "status"
equals = "error"
