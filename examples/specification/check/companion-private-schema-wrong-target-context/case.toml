command = ["check", "--json", "other.test.veln", "math.veln"]
exit = 1

[stdout]
format = "json"
contains = ["\"id\":\"schema.decode_expression\"", "\"id\":\"schema.composition_reference\"", "schema decode expression schema `math::Packet` is private", "schema field `nested` cannot compose `math::Packet` because it resolves to a private imported schema", "\"reason\":\"private_schema\"", "\"companion_module\":\"other__test_companion\"", "\"companion_target_module\":\"other\"", "\"kind\":\"companion_target\"", "This test companion may access private schemas only from target module `other`."]

[stderr]
format = "empty"

[[json_assert]]
path = "status"
equals = "error"
