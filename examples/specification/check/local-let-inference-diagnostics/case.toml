command = ["check", "--json", "main.veln"]
exit = 1

[stdout]
format = "json"
contains = ["\"id\":\"type.local_inference_incomplete\"", "\"inferred_type\":\"Vec<unknown>\"", "\"inferred_type\":\"List<unknown>\"", "\"inferred_type\":\"Dict<unknown, unknown>\"", "\"id\":\"type.inference_ambiguous\"", "\"id\":\"type.mismatch\"", "expected `Vec<String>`, but found `Vec<Int>`", "expected `fn(unknown) -> Vec<Int>`, but found `fn(Int) -> Vec<String>`"]

[stderr]
format = "empty"

[[json_assert]]
path = "status"
equals = "error"

[[json_assert]]
path = "summary.diagnostic_count"
equals = 8

[[diagnostics]]
id = "type.local_inference_incomplete"
severity = "error"
kind = "type"
message = "omitted local binding `items` has no concrete inferred type"

[diagnostics.span]
file = "main.veln"
line = 11
column = 6

[[diagnostics]]
id = "type.local_inference_incomplete"
severity = "error"
kind = "type"
message = "omitted local binding `items` has no concrete inferred type"

[diagnostics.span]
file = "main.veln"
line = 16
column = 6

[[diagnostics]]
id = "type.mismatch"
severity = "error"
kind = "type"
message = "expected `Vec<String>`, but found `Vec<Int>`"

[diagnostics.span]
file = "main.veln"
line = 23
column = 17

[[diagnostics]]
id = "type.inference_ambiguous"
severity = "error"
kind = "type"
message = "empty Vec literal needs concrete type context"

[diagnostics.span]
file = "main.veln"
line = 27
column = 31

[[diagnostics]]
id = "type.inference_ambiguous"
severity = "error"
kind = "type"
message = "constructor `Nil` needs type context"

[diagnostics.span]
file = "main.veln"
line = 28
column = 39

[[diagnostics]]
id = "type.inference_ambiguous"
severity = "error"
kind = "type"
message = "empty Dict literal needs concrete type context"

[diagnostics.span]
file = "main.veln"
line = 29
column = 36

[[diagnostics]]
id = "type.local_inference_incomplete"
severity = "error"
kind = "type"
message = "omitted local binding `mapped` has no concrete inferred type"

[diagnostics.span]
file = "main.veln"
line = 42
column = 6

[[diagnostics]]
id = "type.mismatch"
severity = "error"
kind = "type"
message = "expected `fn(unknown) -> Vec<Int>`, but found `fn(Int) -> Vec<String>`"

[diagnostics.span]
file = "main.veln"
line = 47
column = 15
