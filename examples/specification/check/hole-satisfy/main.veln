// Typed holes can carry a satisfy predicate that constrains repair candidates.
fn select(fallback: Int, spare: Int) -> Int
require fallback >= 0
  _choice satisfy candidate => candidate >= fallback
end
