schema UnsupportedDictListOptionPacket
	items: List<Option<Dict<String, Int>>>
end

fn direct_dict(packet: {items: List<Option<Dict<String, Int>>>}) -> Result<{items: List<Option<Dict<String, Int>>>}, String>
	byte_encode_unsupported_dict_list_option_packet(packet)
end

fn explicit_dict(packet: {items: List<Option<Dict<String, Int>>>}) -> Result<{items: List<Option<Dict<String, Int>>>}, String>
	encode UnsupportedDictListOptionPacket from packet
end

schema UnsupportedResultListOptionPacket
	items: List<Option<Result<Int, String>>>
end

fn direct_result(packet: {items: List<Option<Result<Int, String>>>}) -> Result<{items: List<Option<Result<Int, String>>>}, String>
	byte_encode_unsupported_result_list_option_packet(packet)
end

fn explicit_result(packet: {items: List<Option<Result<Int, String>>>}) -> Result<{items: List<Option<Result<Int, String>>>}, String>
	encode UnsupportedResultListOptionPacket from packet
end
