use helper

type Base
  Base(value: Int)
end

pub type Shared = Base

schema Shared
  value: Int
end

fn local(value: Shared) -> Shared
  value
end

fn imported(value: helper::Shared) -> helper::Shared
  value
end
