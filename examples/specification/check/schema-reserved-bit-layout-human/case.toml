command = ["check", "main.veln"]
exit = 1

[stdout]
format = "text"
contains = ["error[schema.reserved_bits_encode]: `ReservedBits(9, 0)` is outside the supported binary schema field layouts", "note: Schema `UnsupportedReservedSuffix` field `control_padding` declares ReservedBits(9, 0).", "note: Adjacent visible field widths: previous `control` is 8 bit(s).", "note: Supported layout family: packed_reserved_suffix; the previous visible width plus the reserved width must complete one supported big-endian storage unit.", "error[schema.reserved_bits_encode]: `ReservedBits(10, 0)` is outside the supported binary schema field layouts", "note: Adjacent visible field widths: next `control` is 5 bit(s).", "note: Supported layout family: packed_reserved_prefix; the reserved width plus the next visible width must complete one supported big-endian storage unit.", "error[schema.reserved_bits_encode]: `ReservedBits(15, 0)` is outside the supported binary schema field layouts", "note: No adjacent visible exact-width field participates in this unsupported layout.", "note: Supported layout family: bit_packed_reserved_group; a bit-packed group must contain at least one visible field and complete one supported big-endian storage unit."]

[stderr]
format = "empty"
