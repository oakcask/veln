command = ["check", "main.veln"]
exit = 1

[stdout]
contains = ["error[schema.format_neutral_encode_helper]: format-neutral schema field `payload` cannot expose a generated encode helper because `CallbackPayload` is not a recursive format-neutral visible shape", "note: main.veln:5:1: Generated format-neutral encode helpers for schema `BadPacket` accept recursive visible shapes made from Int, Bool, Float, and String leaves", "eligible same-module or public imported source ADTs when every recursively visited child or constructor payload is also eligible"]

[stderr]
format = "empty"
