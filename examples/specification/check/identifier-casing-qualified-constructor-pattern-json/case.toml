command = ["check", "--json", "main.veln"]
exit = 1

[stdout]
format = "json"
equals_file = "case-text/stdout-1.json"
not_contains = ["name.unresolved", "type.mismatch", "type.match_non_exhaustive"]

[stderr]
format = "empty"

[[diagnostics]]
id = "name.invalid_case"
kind = "name"
message = "constructor name `none` must start with an ASCII uppercase letter"
