schema UnsupportedOptionListPacket
	items: List<Option<Int>>
end

fn direct(packet: {items: List<Option<Int>>}) -> Result<{items: List<Option<Int>>}, String>
	byte_encode_unsupported_option_list_packet(packet)
end

fn explicit(packet: {items: List<Option<Int>>}) -> Result<{items: List<Option<Int>>}, String>
	encode UnsupportedOptionListPacket from packet
end
