pub fn first(items: Vec<Int>) -> Int
  vec_len(items)
end

pub fn second(items: Vec<Int>) -> Int
  let callback: fn(Vec<Int>) -> Int = prelude::vec_len
  let bare_callback: fn(Vec<Int>) -> Int = vec_len
  callback(prelude::vec_len(items)) + bare_callback(items)
end
