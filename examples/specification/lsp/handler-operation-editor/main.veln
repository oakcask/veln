effect Ask
  value() -> Int
  add(left: Int, right: Int) -> Int
end

fn provide(ctx:Int,value:Int)->Int
  ctx + value
end

handler ask(ctx:Int) handles Ask
  value()=>provide(ctx,1)
  add(left,right)=>provide(ctx,left + right)
end
