use wire
use facade
use app::nested

## Documents same-module {@schema LocalPacket}, private same-module {@schema PrivatePacket}, imported {@schema wire::PublicPacket}, nested imported {@schema app::nested::NestedPacket}, and alias {@schema facade::AliasPacket}.
pub schema LocalPacket
	format binary

	length: UInt8
end

schema PrivatePacket
	format binary

	length: UInt8
end
