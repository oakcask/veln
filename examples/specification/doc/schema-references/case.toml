command = ["doc", "main.veln", "wire.veln", "facade.veln", "app/nested.veln"]
exit = 0

[stdout]
format = "text"
contains = ["##### schema LocalPacket", "Documents same-module `LocalPacket`, private same-module `PrivatePacket`, imported `wire::PublicPacket`, nested imported `app::nested::NestedPacket`, and alias `facade::AliasPacket`.", "##### schema NestedPacket", "##### schema AliasPacket = wire::PublicPacket"]

[stderr]
format = "empty"
