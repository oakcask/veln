command = ["doc", "main.veln", "wire.veln", "facade.veln"]
exit = 0

[stdout]
format = "text"
contains = ["##### schema LocalPacket", "Documents same-module `LocalPacket`, private same-module `PrivatePacket`, imported `wire::PublicPacket`, and alias `facade::AliasPacket`.", "##### schema AliasPacket = wire::PublicPacket"]

[stderr]
format = "empty"
